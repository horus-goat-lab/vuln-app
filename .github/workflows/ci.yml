  vuln-app (unpinned — makes SC-011 fire):
  name: ci
  on: [push]
  jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4

  hardened-app (SHA-pinned — makes SC-011 pass):
  name: ci
  on: [push]
  jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
